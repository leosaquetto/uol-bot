# UOL quota investigation — 2026-09-08

## Scope

Clube UOL, ticket discovery, Telegram, Discord and WhatsApp. Keep the 15-second
primary poll and existing filters. BuyTicket is unchanged. Do not call or modify
Claro services.

## Evidence

- Authenticated API diagnostics: `uol_api_http_404`; last API success September 4.
  The public ticket listing is healthy and currently contains the Mooca Plaza
  offer already present in the inventory. Empty delivery queues do not prove
  successful discovery.
- WhatsApp: three September 5 ticket records have `SUCCESS` in the Beeper index
  and a matching message in the WhatsApp bridge. No synthetic sends or ambiguous
  resends were performed. This is historical delivery evidence, not proof of a
  new September 8 delivery.
- Initial SQL totals September 8: 1,018,161 reads, 869,632 attributed to maintenance.
- New per-cycle telemetry caught 20,104 reads in a reconciliation, of which
  16,340 came from 48 exact-link conflict lookups (query hash `164841c5`).
- The lookup had no leading `link` index. Migration 26 adds a covering index on
  `(link, first_seen_at, id)` without deleting records or altering identity rules.
- Existing source incident remains visible; API and combined-source success are
  not relabeled healthy just because HTML fallback succeeds.

## Changes

1. Gateway health older than ten minutes is unknown. Refresh via GET every five
   minutes during operational maintenance; persist health checkpoints so restarts
   do not restore a misleading old timestamp.
2. Authenticated `cycleDiagnostics` and sampled structured logs expose bounded
   query hashes, read/write counters and elapsed time. No bind values or raw SQL
   are logged. Diagnostics are in-memory; daily quota counters remain persistent.
3. API errors no longer request immediate global maintenance. Periodic work has
   a persisted cadence guard; duplicate requests coalesce. Fast HTML recovery
   runs independently, shares fetches, skips unchanged fingerprints and preserves
   primary ticket delivery latency. Only complete periodic snapshots infer absence.
4. An additive SQLite index removes the measured full-table URL lookup. Recovery
   respects the existing maintenance read reserve.

## Releases and acceptance

- Stage 1: `cc462460b`, version `d8fa1ebf-72a4-4ca3-9256-a1a086f41602`.
- Stage 2: `164cc2a38`, version `b58465f1-14e7-48b1-90a4-64fe9f3a8b4e`.
- Both passed exact-commit GitHub CI. Postdeploy acceptance remains failed because
  of the pre-existing critical source incident. Do not describe these as fully
  healthy releases.
- Live stage 2 observation: primary 89 reads / 1.4 s, recovery unchanged 0 SQL
  reads before quota checkpoint / 1.3 s, maintenance 176 reads / 1.0 s; no queued
  delivery. These are individual cycles, not daily savings percentages.
- Required follow-up: final-version 30-minute comparison and a complete UTC daily
  cycle; reconcile account duration/requests/SQL metrics on the same time range.
  The specific dimension responsible for the emailed 90% alert is unconfirmed.
- API 404 requires upstream/authenticated endpoint investigation. Do not change
  credentials, suppress incidents or weaken release checks to force acceptance.

Rollback: redeploy the prior reviewed commit through the normal release path.
Migration 26 is additive and compatible with older code; leave its index in place.
Never replay ambiguous deliveries as part of rollback.
