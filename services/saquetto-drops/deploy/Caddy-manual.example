# Add this handle BEFORE the existing gateway fallback in the HTTPS site block.
# Only this namespace is public; Drops administration and pairing stay local.
handle /v1/whatsapp/* {
  request_body {
    max_size 8MB
  }
  reverse_proxy 127.0.0.1:8788
}
